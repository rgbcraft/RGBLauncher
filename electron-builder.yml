appId: 'rgblauncher'
productName: 'RGB Launcher'
artifactName: '${productName}.${ext}'

copyright: 'Copyright © 2023 Enn3DevPlayer'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,.eslintrc.json}'

extraResources:
  - 'libraries'

# Windows Configuration
win:
  target:
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
  #        - 'arm64'
  artifactName: '${productName}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux:
  target:
    - target: 'AppImage'
      arch:
        - x64
    - target: 'deb'
      arch:
        - x64
    - target: 'rpm'
      arch:
        - x64
    - target: 'tar.xz'
      arch:
        - x64
  maintainer: 'Enn3DevPlayer'
  vendor: 'Enn3DevPlayer'
  synopsis: 'RGB Launcher'
  description: 'RGBcraft Launcher.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'